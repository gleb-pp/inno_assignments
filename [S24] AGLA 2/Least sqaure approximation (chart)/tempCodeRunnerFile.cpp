    // for (double x = -1; x <= 50; x += 0.1) {
    //     double y = 0;
    //     for (double i = coef.size() - 1; i >= 0; i--) {
    //         y += coef[i] * pow(x, i);
    //     }
    //     fprintf(pipe, "%f\t%f\n", x, y);
    // }